<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Default margin size used for spacing between UI elements -->
    <dimen name="default_margin">16dp</dimen>

    <!-- Default padding size, referencing the default margin for consistency -->
    <dimen name="default_padding">@dimen/default_margin</dimen>

    <!-- Size of the circular progress indicator used in the server configuration screen -->
    <dimen name="configure_server_indicator_size">24dp</dimen>

    <!-- Thickness of the track for the circular progress indicator in the server configuration screen -->
    <dimen name="configure_server_track_thickness">2dp</dimen>
</resources>
